version: '3'
services:
  rest:
    build: .
    image: monitoring-microservice
    ports:
     - "80:80"
    environment:
     - MONGODB_URL=mongo:27017
  mongo:
    image: mongo:4
    ports:
     - "27017:27017"
    volumes:
     - mongo-data:/data/db
volumes:
  mongo-data: